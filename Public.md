# YaKit-选色开发说明

本仓库提供基于 SillyTavern 背景的 HSL 正文选色脚本，使用原生 JavaScript、CSS、SVG 和 Canvas；Node.js 构建生成可导入的酒馆助手 JSON，无第三方依赖。

## 仓库结构

```text
.
├── .gitignore                 # 忽略本地依赖、环境配置与设计验收资料
├── AGENTS.md                  # 项目约定与酒馆验收流程
├── README.md                  # 用户安装与使用说明
├── Public.md                  # 模块职责、数据流与边界说明
├── package.json               # 版本、运行环境与构建命令
├── dist/
│   └── YaKit-选色.json         # 包含全部资源的可导入脚本
├── icons/
│   ├── palette.svg            # 悬浮入口和窗口图标
│   ├── close.svg              # 关闭图标
│   ├── test.svg               # 检测图标
│   ├── copy.svg               # 复制图标
│   └── check.svg              # 可用状态图标
├── shared/
│   ├── theme/tokens.css       # 复用的 YaKit 林系主题变量
│   └── ui/dialog-motion.js    # 复用的弹窗退出与 Esc 行为
├── src/
│   ├── app.js                 # 脚本启动、检测调度、失效处理与复制
│   ├── background.js          # 宿主背景状态与 Canvas 采样
│   ├── color.js               # HSL、亮度、对比度和可用区间计算
│   └── ui/
│       ├── panel.js           # 悬浮按钮、弹窗与结果展示
│       ├── controls.js        # 检测选项与色区表单
│       ├── panel.css          # 主页尺寸、控件与响应式布局
│       └── motion.css         # 240ms 弹窗动效与减少动态效果适配
└── scripts/
    ├── build.mjs              # 内嵌模块、CSS、SVG 并生成 JSON
    └── check.mjs              # 算法边界和产物格式检查
```

## 加载与数据流

1. `npm run build` 将本项目静态相对导入转换为 `data:` 模块，并内嵌 CSS、SVG；生成 `type: script` 的独立 JSON。
2. 酒馆助手导入后需用户启用，在助手 iframe 内执行入口；界面挂载到 `window.parent.document` 的 Shadow DOM。
3. 入口发出内部销毁事件清理旧实例，创建唯一悬浮按钮；点击后打开原生 `dialog`。
4. 点击测试时校验表单、清除旧结果并禁用表单；读取当前背景，不读取消息正文或调用模型。
5. Canvas 先绘制页面底色和 `#bg1` / `#bg2` 壁纸，再逐个叠加可见 `.mes_text` 的祖先底色；支持透明背景与 Canvas 可处理的 backdrop 滤镜。
6. 壁纸根据实际 `cover`、`contain`、尺寸、位置和常规平铺设置绘制；视口最长边最多采样 480 像素，在可见正文区域取得最低、最高相对亮度。
7. 固定 S/L 后逐度计算 HSL 色相的亮度范围。色相亮度与背景亮度范围相交时，最低对比度按 1 处理；否则取最靠近背景的一端。只有完整一度区间达标才合并输出。
8. 自动模式遍历 10–90 的整数亮度，优先选择已选色区可用角度总数最多的结果，并列选离 50% 最近的亮度。S 保持用户指定值。
9. 输出通过的连续区间、样例和最低对比度；复制文字附带固定 S/L、阈值及当前可见区域限定。
10. 复制时选中输出，在宿主窗口的 JavaScript 上下文调用原生复制命令；不可用时尝试 Clipboard API。失败或超过 1.5 秒未响应时保留选中文本，显示手动复制提示。
11. 样式、消息节点、窗口尺寸和聊天滚动触发背景状态比较。只在实际采样样式或位置改变时清空结果；检测期间有变化则取消本次输出，要求重测。
12. 脚本关闭或重载触发 `pagehide`，移除界面、监听器和观察器。

| 边界 | 处理 |
| --- | --- |
| 未找到 `#chat` | 显示应在酒馆助手脚本库运行的提示 |
| 没有可见消息 | 使用空聊天区底色；若聊天区也不可见则报错 |
| 没勾选色区 | 不执行采样，要求至少勾选一个 |
| 非数字、越界或相同起止值 | 显示对应输入错误，清空旧输出并禁用复制 |
| 起始大于结束 | 分为 `[start, 360]` 与 `[0, end]`，按该顺序输出；`360 → 0` 为空区间，拒绝 |
| 自动模式下 L 为空 | 不使用该值，仍进行自动亮度搜索 |
| 禁用色区的无效输入 | 不参与计算与输出 |
| 色区相互重叠 | 各色区独立计算；自动模式的覆盖角度按各已选色区累计 |
| 无色域达标 | 输出为空，保留未通过的状态，禁止复制 |
| 壁纸不可读或超时 | 8 秒后终止，提示改用可读取的壁纸，不用假定背景生成结果 |
| 渐变、多层图片、受检层的混合或不支持的滤镜 | 明确报错，不输出已通过的范围 |
| 检测过程中背景改变 | 丢弃本次结果，要求重新测试 |
| 剪贴板权限或宿主执行策略受限 | 选中结果，用户自行复制 |

对比度计算依据：[W3C 正文对比度](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum/)。缩小采样可能遗漏微小图案；最暗和最亮亮度之间视为连续背景，可能比单个离散采样点的判断更保守。

## 设置与主题

| 字段 | 默认值 / 含义 |
| --- | --- |
| `saturation` | `70`；固定饱和度，0–100 |
| `lightness` | `50`；手动亮度，0–100；自动模式忽略输入值 |
| `auto` | `true`；遍历 10–90 的整数亮度 |
| `threshold` | `4.5`；允许 `4.5` 或 `7` |
| `groups[].name` | 固定八色名称，由检测层统一恢复 |
| `groups[].start/end` | 0–360 整数，允许跨零 |
| `groups[].enabled` | `true`；控制该色区是否参与本次检测 |

| 默认色区 | 色相 |
| --- | --- |
| 红 | 345–360 或 0–15 |
| 橙 | 15–40 |
| 金黄 | 40–65 |
| 绿 | 65–165 |
| 青 | 165–200 |
| 蓝 | 200–260 |
| 紫 | 260–300 |
| 粉 | 300–345 |

设置保存在当前脚本实例内，关闭弹窗保留，脚本重启恢复默认。没有写入酒馆设置或本地存储。

数字输入框统一隐藏原生上下步进按钮，保留 `type="number"` 的数值校验和键盘操作。

UI 使用从 ST-YaKit-chat 复用的 `--yakit-*` 林系变量；20px 窗口圆角、12px 卡片圆角、56px 顶栏、68px 底栏。宽屏（≥960px）宽 `min(1040px, 92vw)`，其余宽 `min(560px, 92vw)`，高度统一 `min(680px, 100dvh - 24px)`。进入/退出为 240ms，遵循减少动态效果偏好。参考主页改变时同步共享变量、尺寸和动效。

界面主题与被检测的酒馆主题各司其职：显示面板使用林系色，采样使用宿主实际计算样式。

## 公开 API

当前未向其他插件提供业务 API。源码的模块导出服务于本脚本组装，内部销毁事件用于清理重复实例，不作为对外契约。

## 当前接入状态

| 项目 | 状态 |
| --- | --- |
| 酒馆助手 JSON 导入、全局脚本启用 | 已完成，按 4.9.5 导入结构验证 |
| 悬浮入口、原生弹窗、SVG 图标 | 已完成 |
| 单张壁纸与可见正文祖先底色叠加 | 已接入 `#bg1` / `#bg2`、`#chat`、`.mes_text` 和 computed style |
| 自动亮度、手动 S/L、八色自定义与复制 | 已完成 |
| 失效检测、重复实例清理、pagehide | 已完成 |
| 全页面像素级认证 | 未提供；当前为缩小采样估算 |
| 任意自定义 CSS、伪元素遮罩、混合效果 | 未完整支持；可识别的受检层异常会中止 |
| 动图、视频背景的全时间段评估 | 未提供；图片按读取时单帧处理 |
| 更改酒馆主题、注入聊天、调用 AI | 未接入 |
| 持久化设置、在线更新、对外 API | 未提供 |

宿主最低版本沿用酒馆助手声明的 SillyTavern 1.12.13，脚本支持起点设为酒馆助手 4.9.5。浏览器需支持 `dialog.showModal`、Shadow DOM、Canvas 2D、`data:` ES 模块、`MutationObserver`；Clipboard API 受限时可手动复制。复制的宿主上下文调用依赖允许创建函数，受 CSP 限制时同样转为手动复制。

## 开发与验证

1. 使用 Node.js 22+，执行 `npm run build` 生成导入包，无需安装依赖。
2. 执行 `npm run check`，验证黑白对比度、明暗背景、自动亮度、连续区间、跨零、自定义、非法输入和导入格式。
3. 业务变更只改检测、采样或调度模块；UI 变更只改 `src/ui`、`shared` 与图标，分开处理。
4. 改动后重新构建 JSON，先停用旧脚本，再更新并启用，避免运行实例覆盖新内容；更新后核对保存的内容。打包脚本仅服务当前无循环的静态相对导入结构。
5. 按 [AGENTS.md](AGENTS.md) 由人工验收，完成导入、启用、点击悬浮按钮、测试和复制。

v0.1.0 已在本地酒馆助手 4.9.5 完成上述交互；核对了自定义跨零区间与剪贴板文本、越界输入及 7:1 无结果时禁用复制，并检查约 320px 窄屏和 1200px 宽屏下的布局。
